# Archive — notebook đã được thay thế

Các notebook ở đây đã bị **thay thế hoàn toàn** bởi phân tích/báo cáo mới nên tách khỏi thư mục chính cho gọn. Vẫn giữ lại để tham khảo (không xóa). Muốn dùng lại chỉ cần chuyển ngược ra thư mục gốc.

| Notebook | Vai trò cũ | Bị thay bởi |
|---|---|---|
| `salmonella_direction_X.ipynb` | Limitation-driven: mutation-proxy (keyword), bias-audit, feature-dropout. **Chưa bao giờ chạy** (0 output). | `24_lineage.ipynb` (feature-dropout thật) + `26_mutation.ipynb` (mutation calling gyrA/parC thật) |
| `salmonella_direction_W.ipynb` | Consolidation: gom O/R/T/U/V thành báo cáo/bảng/hình. | `FINAL_REPORT.md` (báo cáo cuối hoàn chỉnh A–AD + hình) |
| `salmonella_direction_S.ipynb` | Dò tìm thuốc external từ BV-BRC AMR metadata. | `25_external_mlst.ipynb` (tự fetch nhãn + feature + MLST trực tiếp) |

Ngày archive: 2026-07-09.
